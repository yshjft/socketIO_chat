<!DOCTYPE html>
<html lang="en">
<head>
    <%-include('head')%>
</head>
<body>
    <fieldset>
        <legend>채팅방 생성</legend>
        <form action='/room' method='post'>
            <div>
                <input name='title' placeholder="방 제목" required autofocus/>
            </div>
            <div>
                <input name='max' type='number' placeholder="수용인원 최소 2명" min='2' required/>
            </div>
            <div>
                <input name='password' type='password' placeholder="비밀번호(없으면 공개방)" />
            </div>
            <button type='submit'>생성</button>
        </form>
    </fieldset>
</body>
</html>